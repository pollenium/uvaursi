"use strict";
exports.__esModule = true;
function toArray(u) {
    var array = [];
    u.forEach(function (element) {
        array.push(element);
    });
    return array;
}
exports.toArray = toArray;
